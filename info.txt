proces : is Memory spece that OS creat it for any programe to make execution for it. 
and Proces : is an depandent and self-contained until of execution

A proces has own main thread, so your programe is a thread.

each proces has it's own separat Memory space , the proces do not share it's Memory
with other programe by default 


A thread is the smallest unit of execution within a proces.

Threads of a proces share same resources within the same proces 
Threads can effecintly cummunicate with each other throught same Memory 


